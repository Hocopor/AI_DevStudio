## Live Plan

1. [completed] Изучить текущие точки интеграции для артефактов, GitHub и web_search, зафиксировать требования в live plan mirror.
2. [completed] Реализовать backend: API артефактов/скачивания, безопасную GitHub-интеграцию и fallback для web_search.
3. [completed] Добавить UI для просмотра и скачивания результатов, затем проверить ключевые сценарии и обновить план.

## Notes

- Артефакты уже сохраняются в MinIO, но у пользователя нет нормального пути просмотра и скачивания.
- GitHub-интеграции в проекте пока нет; безопаснее публиковать только явно выбранные артефакты, а не давать агентам прямой доступ ко всему файловому дереву.
- `web_search` сейчас зависит от DuckDuckGo HTML endpoint и периодически ломается на `202 Accepted`; нужен fallback.
- Добавлены новые backend-сущности и роуты под артефакты/GitHub, идёт прогон на сборку и UI-связку.
- Проверка завершена: `py -3 -m compileall backend` проходит, `npm.cmd --prefix frontend run build` проходит.
